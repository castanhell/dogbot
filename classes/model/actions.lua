-- Defines an action

Action = {}

-- Should indicate if the Action hanged
function Action.name()
end

-- Should indicate if the Action hanged
function Action.hanged()
end

-- Should indicate if the Action is complete
function Action.complete()
end
